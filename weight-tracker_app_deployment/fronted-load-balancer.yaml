apiVersion: v1
kind: Service
metadata:
  name: react-frontend-service
  namespace: weight-tracker
spec:
  selector:
    app: react-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30080
  type: NodePort

